{% extends 'base.html.twig' %}

{% block title %}Edit Location{% endblock %}

{% block body %}
    <h1>Edit Location</h1>

     {{ form_start(form) }}
    {{form_rest(form) }}
    <button class="btn-dark btn" type="submit">Éditer</button>
    {{ form_end(form) }}


    <a href="{{ path('app_home') }}" class="btn btn-secondary">Cancel</a>
{% endblock %}
